import type { Campaign } from '../types/content';

// Brand names are known (design-reference.md § 3, step 1: Coca-Cola, Nike,
// Tekme Creatives, TripAdverts) but per-card status/platform/paid-out/rate
// values aren't specified in the source material — populate in Phase 3.
export const campaigns: Campaign[] = [];
